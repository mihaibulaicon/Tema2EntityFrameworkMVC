@model IEnumerable<Tema2.Database.Entitati.Order>

@{
    ViewBag.Title = "Index";
}

<h2>Orders</h2>

<p>
    @Html.ActionLink("Adaugă", "Create")
</p>
<table class="table">
    <tr>
  
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
          <th>
            @Html.DisplayName("Customer")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
       <td>
            @Html.DisplayFor(modelItem => item.Customer.Name)
        </td>
        <td>
            @Html.ActionLink("Editare", "Edit", new { id=item.OrderId }) |
            @Html.ActionLink("Detalii", "Details", new { id=item.OrderId }) |
            @Html.ActionLink("Șterge", "Delete", new { id=item.OrderId })
        </td>
    </tr>
}

</table>
